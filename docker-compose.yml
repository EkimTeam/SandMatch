services:
  # db:  # Локальная БД (не используется с Managed PostgreSQL; оставить как пример)
  #   image: postgres:14
  #   restart: unless-stopped
  #   environment:
  #     POSTGRES_DB: ${POSTGRES_DB:-sandmatch}
  #     POSTGRES_USER: ${POSTGRES_USER:-sandmatch}
  #     POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-sandmatch}
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - db_data:/var/lib/postgresql/data
  web:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8000:8000"
    # volumes:
    #   - .:/app  # Включить после настройки File Sharing (Docker Desktop → Settings → Resources → File Sharing)

volumes:
  db_data:
