# Места турнира и текстовые результаты

Краткая спецификация по новой системе хранения мест турниров и генерации текстовых результатов.

## 1. Хранение мест: TournamentPlacement

Файл: `apps/tournaments/models.py`

Модель `TournamentPlacement`:
- `tournament: FK(Tournament, related_name="placements")`
- `entry: FK(TournamentEntry, related_name="placements")`
- `place_from: int`
- `place_to: int`

Семантика:
- одиночные места: `place_from == place_to` (1, 2, 3, 4 и т.п.);
- диапазоны мест: `place_from < place_to` (5–8, 9–16 и т.п.).

Инвариант: актуальные места для турнира определяются **только** последним пересчётом; перед каждым пересчётом все старые записи по турниру удаляются.

## 2. Пересчёт мест

Файл: `apps/tournaments/services/placements.py`

Главная функция: `recalc_tournament_placements(tournament) -> int`

Поведение:
1. Удаляет все старые `TournamentPlacement` для турнира.
2. В зависимости от системы вызывает специализированный расчёт:
   - ROUND_ROBIN → `_recalc_round_robin_single_group` (одна группа, ранжирование через `rank_group_with_ruleset`).
   - KING → `_recalc_king_placements` (использует `king_stats` для ранжирования).
   - KNOCKOUT → собирает результаты по каждой сетке через `_collect_knockout_results_for_bracket`.
3. Создаёт новые записи через `_create_placements` и возвращает их количество.

### 2.1 Knockout: ключевые правила

Источник: `_collect_knockout_results_for_bracket(bracket)`.

Бизнес‑правила:
- Финал:
  - победитель → 1‑е место;
  - проигравший → 2‑е место.
- Матч за 3‑е (если есть):
  - победитель → 3‑е, проигравший → 4‑е место.
- Если матча за 3‑е нет: оба проигравших полуфиналов получают 3‑е место.
- Проигравшие остальных раундов получают диапазоны мест (5–8, 9–16 и т.п.) в зависимости от размера сетки (`bracket.size`).

Технические детали:
- Финал ищется как матч с максимальным `round_index` среди `is_third_place = False`.
- Если у финала есть `winner_id`, 1‑е и 2‑е места назначаются по `team_id` победителя и проигравшего.
- Если у финала `winner_id` нет (защита от старых/ломаных данных), финалисты берутся как победители полуфиналов; им всё равно ставятся 1‑е и 2‑е места.
- Матч за 3‑е ищется по всем матчам сетки `is_third_place = True`.

## 3. Когда пересчитываются места (UX)

### 3.1 Завершение турнира

Файл: `apps/tournaments/api_views.py`, функция `tournament_complete`.

После перевода турнира в статус `COMPLETED` вызывается `recalc_tournament_placements(t)` в `try/except`.

Решение по UX:
- Завершение турнира **не ломается**, даже если расчёт мест выбросил исключение.
- При успешном вызове пересчитываются **все** места:
  - старые записи `TournamentPlacement` удаляются;
  - новые полностью соответствуют текущему состоянию матчей и участников.
- Если пользователь перерасставил участников/перезаписал счёт и ещё раз завершил турнир, места будут пересчитаны «по‑новому».

### 3.2 Ленивая инициализация при запросе текстовых результатов

Файл: `apps/tournaments/api_views.py`, метод `TournamentViewSet.text_results`.

Если турнир завершён, но `placements` ещё нет (старые данные), то при первом запросе `/api/tournaments/{id}/text_results/` автоматически вызывается `recalc_tournament_placements(tournament)`. В случае ошибки возвращается пустой текст.

Таким образом, для старых турниров места и текстовые результаты появляются «по требованию» без ручных действий.

### 3.3 Админский пересчёт

Базовая команда: `python manage.py recalc_placements`.

Поддерживаются три режима:

1. **Один турнир по ID**

   ```bash
   python manage.py recalc_placements <tournament_id>
   ```

   - Пересчитываются места **только** для указанного турнира.
   - Старые `TournamentPlacement` этого турнира удаляются, создаются новые по текущему состоянию.

2. **Все завершённые турниры**

   ```bash
   python manage.py recalc_placements --all
   ```

   - Выбираются все турниры со статусом `COMPLETED`.
   - Для каждого по очереди вызывается `recalc_tournament_placements`.
   - Удобно для массового пересчёта после изменения алгоритма.

3. **Завершённые турниры до указанной даты**

   ```bash
   python manage.py recalc_placements --before YYYY-MM-DD
   ```

   - Фильтруются только турниры со статусом `COMPLETED` и `date <= YYYY-MM-DD`.
   - Используется, когда нужно переобработать только «старые» турниры (например, до момента фикса бага).

Ограничения и инварианты:
- Нельзя одновременно указывать ID и флаги `--all` / `--before`.
- В любом режиме команда безопасна для повторного запуска: старые места стираются, новые считаются заново.

#### 3.3.1 Примеры использования (how-to)

- **Пересчитать места для одного конкретного турнира** (например, после правки результатов):

  ```bash
  python manage.py recalc_placements 123
  ```

- **Пересчитать места для всех завершённых турниров** (массовый прогон после изменения алгоритма):

  ```bash
  python manage.py recalc_placements --all
  ```

- **Пересчитать места только для старых турниров до 2024-01-01 включительно**:

  ```bash
  python manage.py recalc_placements --before 2024-01-01
  ```

- **Проверить текстовые результаты по турниру после пересчёта**:

  - через браузер: открыть

    ```
    /api/tournaments/<id>/text_results/
    ```

  - через `curl` (если эндпоинт закрыт авторизацией, добавить заголовок с токеном):

    ```bash
    curl -X GET https://<host>/api/tournaments/123/text_results/
    ```

## 4. Текстовые результаты и фронтенд‑UX

### 4.1 Эндпоинт текстовых результатов

Файл: `apps/tournaments/api_views.py`, метод `TournamentViewSet.text_results`.

Формат текста:
- Заголовок: `По итогам соревнований`.
- Далее (если есть дата): `ДД.MM.ГГ:` с пустой строкой сверху.
- Далее (если есть название): название турнира с пустой строкой сверху.
- Затем по одной строке на каждый `TournamentPlacement`:
  - одиночное место: `"{N}-е место {emoji} {team}"`;
  - диапазон: `"{from}-{to}-е места {emoji} {team}"`.

Эмодзи:
- 1‑е место → 🥇
- 2‑е → 🥈
- 3‑е → 🥉
- остальные → 🎉

### 4.2 Кнопки и модалки на фронте

Файлы:
- `frontend/src/pages/TournamentDetailPage.tsx` — круговая система.
- `frontend/src/pages/KnockoutPage.tsx` — олимпийка.
- `frontend/src/pages/KingPage.tsx` — King.

UX:
- Для завершённых турниров отображается кнопка `"Результаты текстом"` рядом с кнопкой `"Поделиться"` (кроме роли `REFEREE`).
- При клике открывается модальное окно с:
  - `textarea` только с текстом результатов (без локального заголовка типа "Результаты текстом" — первое, что видит пользователь, это фраза `По итогам соревнований`).
  - кнопками `"Закрыть"` и `"Копировать"`.
- Кнопка `"Копировать"` копирует весь текст из `textarea` в буфер (через `navigator.clipboard` с fallback).

Решение по UX:
- Источником истины для формата сообщения является бэкенд‑текст; фронт его не модифицирует.
- Отсутствие отдельного заголовка в модалке делает скопированный текст готовым к отправке как есть (первой строкой всегда идёт `По итогам соревнований`).

## 5. Резюме по UX пересчёта мест

- **Всегда пересчитываем с нуля**: при явном завершении турнира и при ручной команде `recalc_placements` старые места полностью стираются и считаются по текущему состоянию.
- **Ленивая инициализация**: для старых завершённых турниров места считаются при первом запросе текстовых результатов.
- **Надёжность завершения турнира**: ошибки расчёта мест не блокируют смену статуса и расчёт рейтинга.
- **Единый текстовый формат и кнопка**: для всех трёх систем (RR, KO, King) используется единый эндпоинт, одинаковый формат текста и одинаковый UX фронтенда.
