# Обновления фронтенда системы регистрации

## Обзор изменений

Обновлён фронтенд Mini App для поддержки новой логики регистрации на турниры.

## Изменённые файлы

### 1. API клиент (`frontend/src/api/miniApp.ts`)

#### Новый метод
```typescript
async registerSingle(tournamentId: number): Promise<TournamentRegistration>
```
Простая регистрация для индивидуальных турниров.

#### Обновлённые методы
```typescript
// Было: partnerId: number
// Стало: partnerSearch: string
async registerWithPartner(tournamentId: number, partnerSearch: string)

// Было: receiverId: number
// Стало: receiverSearch: string
async sendPairInvitation(tournamentId: number, receiverSearch: string, message?: string)
```

### 2. Модальное окно регистрации (`frontend/src/components/MiniApp/RegistrationModal.tsx`)

#### Новый prop
```typescript
isIndividual: boolean  // Определяет тип турнира
```

#### Новый режим
- `'single'` - для индивидуальных турниров
- Показывает простую форму регистрации без выбора режима

#### Обновлённая логика
- **Индивидуальный турнир**: сразу показывается кнопка "Зарегистрироваться"
- **Парный турнир**: выбор между "Ищу пару" и "С напарником"

#### Поиск по ФИО
- Поле ввода изменено с `number` (ID) на `text` (ФИО)
- Placeholder: "Иванов Иван"
- Подсказка: "Введите фамилию и имя напарника для поиска"

#### Обработка ошибок
```typescript
// Обработка множественных результатов поиска
if (errorData?.players && Array.isArray(errorData.players)) {
  const playersList = errorData.players.map((p: any) => p.full_name).join('\n')
  alert(`❌ Найдено несколько игроков. Уточните запрос:\n\n${playersList}`)
}
```

### 3. Страница деталей турнира (`frontend/src/pages/MiniApp/MiniAppTournamentDetail.tsx`)

#### Передача типа турнира
```typescript
<RegistrationModal
  ...
  isIndividual={tournament.system === 'single'}
  ...
/>
```

Определение типа турнира по полю `tournament.system`:
- `'single'` - индивидуальный
- `'doubles'` или другое - парный

## UI изменения

### Индивидуальный турнир

**До:**
```
Выберите способ регистрации:
[ ] Ищу пару
[ ] С напарником
```

**После:**
```
🎾 Индивидуальный турнир
Вы будете зарегистрированы как одиночный участник

[Зарегистрироваться]
```

### Парный турнир - регистрация с напарником

**До:**
```
ID напарника: [____]
Подсказка: ID можно узнать в профиле игрока
```

**После:**
```
ФИО напарника: [Иванов Иван]
Подсказка: Введите фамилию и имя напарника для поиска
```

## Сценарии использования

### 1. Регистрация на индивидуальный турнир

1. Пользователь открывает турнир
2. Нажимает "Зарегистрироваться"
3. Видит информацию об индивидуальном турнире
4. Нажимает "Зарегистрироваться"
5. Получает подтверждение

### 2. Регистрация с напарником (поиск по ФИО)

1. Пользователь выбирает "С напарником"
2. Вводит ФИО: "Иванов"
3. Нажимает "Зарегистрироваться"

**Сценарий A: Найден один игрок**
- Регистрация успешна
- Напарнику отправлено уведомление

**Сценарий B: Найдено несколько игроков**
- Показывается список найденных:
  ```
  ❌ Найдено несколько игроков. Уточните запрос:
  
  Иванов Иван Иванович
  Иванов Игорь Петрович
  ```
- Пользователь уточняет запрос

**Сценарий C: Игрок не найден**
- Показывается ошибка: "❌ Игрок не найден"

## Ограничения текущей реализации

### 1. Приглашение в пару
Пока работает только для игроков из списка "Ищут пару" (через компонент `TournamentParticipants`).

Для приглашения любого игрока нужен отдельный UI с поиском:
- Кнопка "Пригласить игрока"
- Модальное окно с поиском по ФИО
- Список результатов для выбора

### 2. Автодополнение
Нет автодополнения при вводе ФИО. Пользователь должен вводить полное имя.

**Возможное улучшение:**
- Добавить API эндпоинт для поиска игроков
- Реализовать автодополнение с выпадающим списком
- Показывать рейтинг и количество турниров

## Будущие улучшения

### 1. Компонент поиска игроков
```typescript
<PlayerSearchInput
  onSelect={(player) => handlePlayerSelected(player)}
  placeholder="Поиск игрока..."
/>
```

Функционал:
- Поиск по мере ввода (debounce 300ms)
- Показ результатов в выпадающем списке
- Отображение аватара, ФИО, рейтинга
- Клавиатурная навигация

### 2. Улучшенная обработка ошибок
```typescript
if (errorData?.players) {
  // Показать модальное окно с выбором игрока
  setMultiplePlayersModal({
    players: errorData.players,
    onSelect: (playerId) => retryWithPlayer(playerId)
  })
}
```

### 3. История поиска
- Сохранение последних поисков в localStorage
- Быстрый выбор из недавних напарников

### 4. Валидация ввода
- Минимум 2 символа для поиска
- Предупреждение о слишком общем запросе
- Подсказки по формату ввода

## Тестирование

### Индивидуальный турнир
1. Создать турнир с `system='single'`
2. Открыть в Mini App
3. Проверить, что показывается простая форма регистрации
4. Зарегистрироваться
5. Проверить создание `Team` с `player_2=NULL`

### Парный турнир - поиск по ФИО
1. Создать турнир с `system='doubles'`
2. Выбрать "С напарником"
3. Ввести "Иванов"
4. Проверить обработку:
   - 0 результатов
   - 1 результат
   - >1 результата

### Обработка ошибок
1. Ввести несуществующее имя
2. Проверить сообщение об ошибке
3. Ввести общее имя (например, "Иван")
4. Проверить список для уточнения

## Совместимость

### Breaking Changes
⚠️ API изменения требуют обновления:
- `partner_id` → `partner_search`
- `receiver_id` → `receiver_search`

### Обратная совместимость
Старые версии Mini App не будут работать с новым API.

Рекомендуется:
1. Обновить версию Mini App
2. Показать пользователям уведомление об обновлении
3. Принудительно обновить при следующем открытии

## Следующие шаги

1. **Реализовать компонент поиска игроков**
   - API эндпоинт `/api/mini-app/players/search/?q=Иванов`
   - Компонент с автодополнением
   - Интеграция в модальное окно

2. **Добавить приглашение любого игрока**
   - Кнопка "Пригласить игрока" в деталях турнира
   - Модальное окно с поиском
   - Отправка приглашения выбранному игроку

3. **Улучшить UX**
   - Показывать количество найденных игроков
   - Добавить индикатор загрузки при поиске
   - Кэшировать результаты поиска

4. **Тестирование**
   - Unit тесты для компонентов
   - E2E тесты для сценариев регистрации
   - Тестирование на разных устройствах
