# 📱 Оптимизация Drag-and-Drop для мобильных устройств

## 🎯 Проблема

На мобильных устройствах HTML5 Drag and Drop API не работает с touch событиями. Пользователи не могли перетаскивать участников в олимпийскую сетку на телефонах и планшетах.

## ✅ Решение

Реализована полная поддержка touch events для drag-and-drop функционала с сохранением совместимости с desktop версией.

---

## 🔧 Внесенные изменения

### 1. **DraggableParticipantList.tsx** - Поддержка touch events

#### Добавлено:
- **Touch события**: `onTouchStart`, `onTouchMove`, `onTouchEnd`
- **Состояние перетаскивания**: `draggedParticipant` для отслеживания текущего элемента
- **Глобальная передача данных**: через `window.__draggedParticipant` для связи между компонентами
- **Визуальная обратная связь**: изменение opacity и класса `.dragging`
- **Предотвращение скролла**: `e.preventDefault()` в `handleTouchMove`

#### Логика работы:
```typescript
handleTouchStart -> сохраняем участника, меняем opacity
handleTouchMove -> предотвращаем скролл страницы
handleTouchEnd -> находим drop-зону, триггерим custom event
```

### 2. **RoundComponent.tsx** - Обработка touch drop

#### Добавлено:
- **Custom event listener**: `participant-drop` для получения данных от touch
- **Data-атрибуты**: `data-drop-slot`, `data-match-id`, `data-slot` для идентификации зон
- **useEffect hook**: подписка на custom events для каждой drop-зоны
- **Автоматическая очистка**: removeEventListener при размонтировании

#### Логика работы:
```typescript
useEffect -> подписываемся на 'participant-drop' event
handleTouchDrop -> получаем данные из event.detail
onDrop -> вызываем существующий обработчик
```

### 3. **knockout-dragdrop.css** - Стили для touch устройств

#### Добавлено:

**Для элементов участников:**
```css
touch-action: none;           /* Отключаем стандартные жесты */
user-select: none;            /* Отключаем выделение текста */
-webkit-user-select: none;    /* Safari */
-webkit-touch-callout: none;  /* iOS callout menu */
```

**Визуальные эффекты:**
```css
.participant-item.dragging {
  opacity: 0.5;
  background: #e3f2fd;
  transform: scale(1.05);      /* Увеличение при перетаскивании */
  box-shadow: 0 4px 8px rgba(0,0,0,0.2);
}
```

**Drop-зоны:**
```css
[data-drop-slot] {
  position: relative;
  touch-action: auto;          /* Разрешаем стандартные жесты */
}

[data-drop-slot]:empty::after {
  content: 'Перетащите сюда';  /* Desktop */
}

@media (max-width: 768px) {
  [data-drop-slot]:empty::after {
    content: 'Нажмите на участника'; /* Mobile */
  }
}
```

**Адаптивность для мобильных:**
```css
@media (max-width: 768px) {
  .knockout-content {
    flex-direction: column;    /* Вертикальная раскладка */
  }
  
  .participants-panel {
    width: 100%;
    max-height: 40vh;          /* Ограничение высоты */
    border-bottom: 2px solid #e0e0e0;
  }
  
  .participant-item {
    padding: 12px 16px;        /* Больше padding для пальцев */
    font-size: 16px;           /* Крупнее текст */
  }
  
  [data-drop-slot] {
    min-height: 48px;          /* Минимум 48px для touch target */
  }
  
  .participant-item:active {
    transform: scale(0.98);    /* Визуальный feedback */
    background: #e3f2fd;
  }
}
```

---

## 🎨 UX улучшения

### Desktop (мышь):
- ✅ Стандартный HTML5 drag-and-drop
- ✅ Курсор `grab` / `grabbing`
- ✅ Hover эффекты
- ✅ Подсказка "Перетащите сюда"

### Mobile (touch):
- ✅ Touch events (touchstart, touchmove, touchend)
- ✅ Визуальная обратная связь (opacity, scale, shadow)
- ✅ Предотвращение скролла при перетаскивании
- ✅ Увеличенные touch targets (48px минимум)
- ✅ Вертикальная раскладка (список сверху, сетка снизу)
- ✅ Подсказка "Нажмите на участника"
- ✅ Крупнее шрифты и отступы

---

## 📋 Как это работает

### 1. Пользователь начинает перетаскивание (Desktop)
```
onDragStart -> setData -> effectAllowed = 'move'
```

### 2. Пользователь начинает перетаскивание (Mobile)
```
onTouchStart -> setDraggedParticipant -> window.__draggedParticipant
              -> opacity = 0.5
```

### 3. Перемещение (Mobile)
```
onTouchMove -> preventDefault() (отключаем скролл)
```

### 4. Отпускание (Desktop)
```
onDrop -> getData -> JSON.parse -> onDrop(matchId, slot, participant)
```

### 5. Отпускание (Mobile)
```
onTouchEnd -> elementFromPoint -> closest('[data-drop-slot]')
           -> CustomEvent('participant-drop', { detail: {...} })
           -> handleTouchDrop -> onDrop(matchId, slot, participant)
```

---

## 🧪 Тестирование

### Desktop:
- [x] Перетаскивание мышью работает
- [x] Hover эффекты отображаются
- [x] Drop-зоны подсвечиваются
- [x] Удаление участников работает

### Mobile:
- [x] Touch перетаскивание работает
- [x] Визуальная обратная связь есть
- [x] Скролл не мешает перетаскиванию
- [x] Drop-зоны достаточно большие
- [x] Вертикальная раскладка удобна

### Браузеры:
- [x] Chrome Desktop
- [x] Chrome Mobile
- [x] Safari Desktop
- [x] Safari iOS
- [x] Firefox Desktop
- [x] Firefox Mobile

---

## 🔄 Обратная совместимость

- ✅ Desktop функционал не изменился
- ✅ Существующий код не сломан
- ✅ Все props и callbacks работают как раньше
- ✅ Добавлены только новые touch обработчики

---

## 📝 Рекомендации для будущего

### Возможные улучшения:
1. **Haptic feedback** на iOS (вибрация при drop)
2. **Drag preview** - показывать элемент под пальцем
3. **Multi-touch** - перетаскивание нескольких участников
4. **Undo/Redo** - отмена последнего действия
5. **Анимация** - плавное перемещение в drop-зону

### Библиотеки для рассмотрения:
- `react-beautiful-dnd` - полнофункциональный drag-and-drop
- `react-dnd` - гибкая библиотека с touch поддержкой
- `@dnd-kit/core` - современная альтернатива

---

## 🐛 Известные ограничения

1. **Глобальная переменная**: Используется `window.__draggedParticipant` для передачи данных между компонентами. Это не идеально, но работает.

2. **Custom events**: Требуется ручная подписка через `addEventListener`. В будущем можно перейти на Context API или Redux.

3. **iOS Safari quirks**: Некоторые CSS свойства могут работать по-разному. Протестировано и работает, но могут быть edge cases.

---

---

## 🎨 Обновление: Визуальный Ghost Element (27.11.2025)

### Проблема:
При перетаскивании на мобильных устройствах не было видно, что именно перетаскивается. Палец двигался по экрану без визуальной обратной связи.

### Решение:

#### 1. **Drag Ghost Element**
Добавлен плавающий элемент, который следует за пальцем:

```tsx
{draggedParticipant && ghostPosition && (
  <div className="drag-ghost" style={{
    position: 'fixed',
    left: ghostPosition.x,
    top: ghostPosition.y,
    transform: 'translate(-50%, -50%)',
    pointerEvents: 'none',
    zIndex: 9999,
    background: 'white',
    border: '2px solid #2196f3',
    borderRadius: '4px',
    boxShadow: '0 4px 12px rgba(0,0,0,0.3)',
    opacity: 0.9
  }}>
    {draggedParticipant.name}
    {draggedParticipant.currentRating && (
      <span>{draggedParticipant.currentRating} BP</span>
    )}
  </div>
)}
```

**Особенности:**
- ✅ Следует за пальцем в реальном времени
- ✅ Показывает имя участника и рейтинг
- ✅ Плавная анимация появления
- ✅ Полупрозрачный для видимости под ним
- ✅ `pointer-events: none` - не мешает взаимодействию

#### 2. **Динамическая подсветка drop-зон**

**При перемещении пальца:**
```typescript
const elementUnderFinger = document.elementFromPoint(touch.clientX, touch.clientY);
const dropSlot = elementUnderFinger?.closest('[data-drop-slot]');

// Подсвечиваем drop-зону под пальцем
if (dropSlot && !dropSlot.querySelector('.participant-name')) {
  dropSlot.classList.add('hover-highlight');
}
```

**Стили подсветки:**
```css
[data-drop-slot].hover-highlight {
  background: #dbeafe !important;
  border-color: #2563eb !important;
  border-width: 2px !important;
  box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.2) !important;
  transform: scale(1.02);
  transition: all 0.15s ease-out;
}
```

#### 3. **Глобальная подсветка всех доступных зон**

При начале перетаскивания добавляется класс `dragging` к `<body>`:

```css
body.dragging [data-drop-slot]:not([data-occupied="true"]) {
  background: #f0f9ff !important;
  border-color: #3b82f6 !important;
  animation: pulse 1.5s ease-in-out infinite;
}
```

**Эффект:**
- Все пустые drop-зоны начинают пульсировать
- Пользователь сразу видит, куда можно бросить участника
- Занятые зоны не подсвечиваются

#### 4. **Анимация появления Ghost**

```css
.drag-ghost {
  animation: ghostAppear 0.2s ease-out;
}

@keyframes ghostAppear {
  from {
    opacity: 0;
    transform: translate(-50%, -50%) scale(0.8);
  }
  to {
    opacity: 0.9;
    transform: translate(-50%, -50%) scale(1);
  }
}
```

### Результат:

**До улучшения:**
- ❌ Палец двигается по экрану без визуальной обратной связи
- ❌ Непонятно, что перетаскивается
- ❌ Не видно, куда можно бросить

**После улучшения:**
- ✅ Плавающий элемент с именем участника следует за пальцем
- ✅ Drop-зона под пальцем подсвечивается синим
- ✅ Все доступные зоны пульсируют
- ✅ Плавные анимации и transitions
- ✅ Четкая визуальная обратная связь

### UX Flow:

1. **Touch Start** → Ghost появляется с анимацией, все пустые зоны начинают пульсировать
2. **Touch Move** → Ghost следует за пальцем, зона под пальцем подсвечивается сильнее
3. **Touch End** → Ghost исчезает, подсветки убираются, участник перемещается

---

**Дата реализации:** 27 ноября 2025  
**Последнее обновление:** 27 ноября 2025 (добавлен Ghost Element)  
**Статус:** ✅ Готово к использованию
